# NEXT INSTANCE STATE

**Last Updated:** 2026-04-30
**Actor:** ORCHESTRATOR
**Current Decomposition:** `docs/_Decomposition/SOFTWARE_DECOMP.md` revision `0.4`
**Current Mode:** full SOFTWARE setup workflow for `PKG-01` through `PKG-12`

## Current Instruction

Run the full SOFTWARE setup workflow for all deliverables in `PKG-01` through `PKG-12`, starting with `four-documents`.

This run intentionally includes `PKG-02`; existing `PKG-02` four-document and semantic artifacts may be overwritten by the current workflow.

Do not rerun `PKG-00` unless the human explicitly requests it. Treat `PKG-00` as prior completed architecture-runway evidence, subject to evidence verification.

## First Required Action

Before planning, dispatching TASKs, editing files, or summarizing state, complete the required read list in `init/NEXT_SESSION_PROMPT.md` and `execution/_Coordination/NEXT_INSTANCE_PROMPT.md`. After reading, state that the reading task is complete and explain the intended workflow from that context.

## Actual Current Coverage Before This Run

| Item | Coverage |
|---|---:|
| Deliverables | 73 |
| Four-document kits | 13 / 73 |
| `_SEMANTIC.md` | 73 / 73, including placeholders where not yet generated by TASK |
| `_SEMANTIC_LENSING.md` | 13 / 73 |
| `Dependencies.csv` | 0 / 73 |
| `_DEPENDENCIES.md` | 73 / 73 |
| Lifecycle `SEMANTIC_READY` | 13 / 73 |
| Lifecycle `OPEN` | 60 / 73 |

The 13 complete kits are `PKG-00` and `PKG-02`. The next run overwrites `PKG-02` and processes `PKG-01` through `PKG-12`.

## Required Workflow

For each deliverable in `PKG-01` through `PKG-12`, ORCHESTRATOR must dispatch bounded one-deliverable TASK/subagent work in this sequence:

1. `four-documents` with `RUN_PASSES=P1_P2`
2. `semantic-matrix-build`
3. `lens-register`
4. `four-documents` with `RUN_PASSES=P3_ONLY`
5. `dependency-extract`

Then run REVIEW, RECONCILIATION, and AUDIT before implementation resumes.

## Guardrails

- No product implementation.
- No bulk scripts or synthetic materializers in place of TASK/subagent execution.
- No ORCHESTRATOR-authored deliverable production content.
- One deliverable per TASK dispatch.
- Explicit sealed scope and write scope for every TASK.
- No `ISSUED` lifecycle changes.
- Preserve protected-content, private-data, and no-certification boundaries.

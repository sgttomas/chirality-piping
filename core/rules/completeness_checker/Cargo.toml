[package]
name = "open_pipe_stress_completeness_checker"
version = "0.1.0"
edition = "2021"
description = "Required-input completeness checker for OpenPipeStress rule packs"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]

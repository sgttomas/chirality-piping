[package]
name = "open_pipe_stress_straight_pipe"
version = "0.1.0"
edition = "2021"
description = "Straight pipe element mechanics for OpenPipeStress"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
open_pipe_stress_frame_kernel = { path = "../frame_kernel" }
